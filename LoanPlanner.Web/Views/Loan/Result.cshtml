@model LoanPlanner.Core.Models.LoanInput
@{
    ViewData["Title"] = "Loan Calculation Result";
    var schedule = ViewBag.Schedule as List<LoanPlanner.Core.Models.PaymentSchedule>;
}

<!DOCTYPE html>
<html>
<head>
    <title>Loan Calculation Result</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <h2>Loan Calculation Result</h2>
        <p><strong>Сумма займа:</strong> @Model.Amount</p>
        <p><strong>Срок займа:</strong> @Model.Term</p>
        <p><strong>Ставка:</strong> @Model.InterestRate%</p>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Месяц</th>
                    <th>Размер платежа</th>
                    <th>Размер платежа по телу</th>
                    <th>Размер платежа по %</th>
                    <th>Остаток основного долга</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var payment in schedule)
                {
                    <tr>
                        <td>@payment.Month</td>
                        <td>@payment.Payment</td>
                        <td>@payment.Principal</td>
                        <td>@payment.Interest</td>
                        <td>@payment.Balance</td>
                    </tr>
                }
            </tbody>
        </table>
        <a asp-action="Index" asp-controller="Loan" class="btn btn-secondary">Back to Calculator</a>
        <a asp-action="Index" asp-controller="DailyLoan" class="btn btn-secondary">Go to Monthly Calculator</a>
    </div>
</body>
</html>